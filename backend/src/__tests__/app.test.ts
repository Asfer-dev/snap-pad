// backend/src/__tests__/app.test.ts

describe('Initial Test Suite Setup', () => {
  // 1. A pure unit test to verify basic assertions
  test('Math check: expect 1 + 1 to equal 2', () => {
    expect(1 + 1).toBe(2);
  });
});
